name: Release
on:
  push:
    tags: ["v*"]

env:
  REGISTRY: ghcr.io
  OWNER: brendatamaar

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v3

      - name: Build & push UI
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.ui
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/invoke-ui:${{ github.ref_name }}
            ghcr.io/${{ env.OWNER }}/invoke-ui:latest

      - name: Scan UI image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.OWNER }}/invoke-ui:${{ github.ref_name }}
          format: table
          exit-code: 1
          severity: HIGH,CRITICAL

      - name: Build & push server
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.server
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/invoke-server:${{ github.ref_name }}
            ghcr.io/${{ env.OWNER }}/invoke-server:latest

      - name: Scan server image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.OWNER }}/invoke-server:${{ github.ref_name }}
          format: table
          exit-code: 1
          severity: HIGH,CRITICAL

      - name: Build & push executor
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.executor
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/invoke-executor:${{ github.ref_name }}
            ghcr.io/${{ env.OWNER }}/invoke-executor:latest

      - name: Scan executor image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.OWNER }}/invoke-executor:${{ github.ref_name }}
          format: table
          exit-code: 1
          severity: HIGH,CRITICAL

      - name: GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
